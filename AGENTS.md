# 자취도우미 AI 작업 규칙

- 이 폴더는 자취도우미 전용 프로젝트다.
- 제품 맥락은 `README.md`, `docs/product-vision.md`, 작업과 직접 관련된 문서 순서로 읽는다.
- RelayOps의 코드·문서·데이터를 가져오거나 수정하지 않는다.
- 검증 명령은 이 폴더에서 `npm run check`로 실행한다.
- 환경변수와 수집 데이터는 각각 `.env`, `data/exports/`에만 저장한다.
